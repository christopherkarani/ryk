{
  "id": "ryk",
  "name": "ryk",
  "version": "1.2.14",
  "description": "Runtime guardrails for OpenClaw workflows via the ryk CLI.",
  "activation": {
    "onCapabilities": ["hook", "tool"]
  },
  "contracts": {
    "tools": ["ryk_openclaw_canary"]
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "workspaceRoot": {
        "type": "string",
        "minLength": 1,
        "description": "Canonical workspace root configured by ryk agents setup. Tool input cannot override it."
      }
    },
    "required": ["workspaceRoot"],
    "additionalProperties": false
  }
}
