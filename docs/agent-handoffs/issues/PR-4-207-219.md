# PR-4 handoff — #207 leftover help + #219 start first-run copy

**Priority:** P2  
**Prefer after:** PR-3 (so nexts match daemon honesty)  
**Parallel with:** PR-5 once PR-3 landed or if carefully avoiding start.zig overlap

## Problems (verified)

**#219 (REAL):** First create still prints “Existing policy files are kept…” / “Existing policy is preserved…” then `Creating .ryk/policy.yaml...`. After verify: `Setup complete — activation evidence pending` / `not applicable`. Tests expect that jargon.

**#207 (PARTIAL):** Ask-vs-DCG on `help start` is **already fixed**. Still real: public help two nexts (`start` vs `help --all`); Common tasks “Get protected” → `doctor --fix`.

## Acceptance

1. First-run create: what it did (policy written, verify passed); no “existing preserved” on create; no activation-evidence jargon.
2. Public help: one coherent next (`ryk <agent>` preferred per Chris cut); fix Get protected teaching.
3. Do not reopen #144/#193; do not refile #205/#208/#215.

## Primary files

`src/cli/start.zig`, `src/cli/onboarding.zig`, `src/cli/help.zig`, `src/cli/mod.zig` (help contract tests)

## Branch

`cursor/fix-207-219-first-run-copy-8968`
