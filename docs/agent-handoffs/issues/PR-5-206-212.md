# PR-5 handoff — #206 stop advertises cursor + #212 plugin omits grok/pi

**Priority:** P2  
**Parallel:** yes (after or beside Phase 2; watch `help.zig` conflicts with PR-4)

## Problems (verified)

**#206:** Launch forbids `ryk cursor` (`host_launch_aliases` has no cursor). `ryk stop --help` still lists cursor in usage/examples/hosts. Tests **require** `"cursor"` in stop help. Uninstall dry-run also lists cursor.

**#212:** Plugin surfaces list only `codex|claude|opencode|openclaw|hermes`. Launch has grok+pi; hook has grok + honest pi extension-only note. Plugin has no honest one-liner. Copy-only — do not invent fake plugin-pi.

## Acceptance

1. Stop/uninstall host lists match launch reality (drop cursor advertising).
2. Plugin help: one honest host list; if grok/pi cannot wire via plugin, say so in one line (hook-pi voice).
3. Update tests that currently lock the lie.
4. One PR, copy/honesty only.

## Primary files

`src/cli/help.zig`, `src/cli/disable.zig`, `src/cli/uninstall.zig`, `src/cli/plugin.zig`, `src/cli/host_launch.zig`, `src/cli/hook.zig`

## Branch

`cursor/fix-206-212-host-lists-8968`
