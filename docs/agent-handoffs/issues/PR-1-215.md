# PR-1 handoff — #215 drop shield banner (closes #213)

**Priority:** P2  
**Parallel:** yes (with PR-0, PR-2, PR-6)  
**Closes after merge:** #213 as duplicate  
**Do not fold:** packs dump (#208), #145 SHIELD UP session card

## Problem (verified)

`writeInvocationPresentation` prints `🛡  ryk · v…` + dash rule for almost every HUMAN command **before** dispatch. `shouldShowBanner` ignores `--quiet`, pipe, errors. Tests **require** banner on doctor and on usage errors.

#213 is the same gate on `ryk init --quiet` (init correctly quiets Created/Next after the banner already printed).

## Acceptance

1. No banner on success, `--quiet`, `--no-rich`, pipe, or errors.
2. Brand may remain on `ryk --version` and optionally first-run `ryk help` only.
3. Rewrite banner unit tests in `src/cli/mod.zig` (do not sneak quiet-only-on-init).
4. Comment on #213: closed as duplicate of #215.

## Primary files

`src/cli/mod.zig` (`shouldShowBanner`, `writeInvocationPresentation`), possibly `src/tui/render.zig`, `src/cli/version.zig`, `src/cli/help.zig`

## Branch

`cursor/fix-215-banner-8968`
