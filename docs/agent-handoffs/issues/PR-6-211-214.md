# PR-6 handoff — #211 env --help + #214 policy explain bare usage

**Priority:** P2  
**Parallel:** yes — safe in the Phase 0 wave

## Problems (verified)

**#211:** `ryk env --help` routes to `env_schema` which only accepts `schema --agent` → prints `Usage: ryk env schema --agent` + exit 2. Real help is only via `ryk help env`.

**#214:** Bare `ryk policy explain` → `expected a type and target.` exit 2, no usage. Contrast: bare `ryk test` prints Usage.

**Skip #204:** telemetry `--help` already fixed on main.

## Acceptance

1. `ryk env --help` / `-h` → real env help, exit 0.
2. Bare `ryk policy explain` prints usage (types listed like `--help`), same voice as `ryk test`.
3. Tests for both.

## Primary files

`src/cli/mod.zig`, `src/cli/env_schema.zig`, `src/cli/policy.zig`, `src/cli/help.zig`

## Branch

`cursor/fix-211-214-help-exits-8968`
