# PR-0 handoff — #221 One-click `ryk grok` never reaches the agent

**Priority:** P1  
**Parallel:** yes (with PR-1, PR-2, PR-6)  
**Sequential blockers:** none  
**Do not fold:** #215, #220, #145, #195, #196

## Problem (verified in code)

`ryk grok` → host launch → `ryk run`. Session start still renders SHIELD UP card + session grade + optional `audit=degraded`, then can fail with config EACCES and a long “empty backpack” tip. Agent never starts.

## Evidence

- `src/cli/host_launch.zig` — `grok` alias
- `src/cli/run.zig` — SHIELD UP / grade / degraded (~2219–2282); tests expect walls (~7033+)
- `src/tui/sandbox_card.zig` — SHIELD UP card
- `src/sandbox/host_config_grants.zig` — tip novel on Permission denied; grok grants narrow (not whole `~/.grok`)

## Acceptance

1. Repro on clean dir without `ryk start`: either Grok starts protected, **or** one short error + one verified next.
2. No SHIELD UP box-drawing card on this first-user path.
3. No tip novel / `audit=degraded` on the success path.
4. Unit/integration tests updated; do not weaken sandbox without a deliberate grant.

## Primary files

`src/cli/run.zig`, `src/cli/host_launch.zig`, `src/sandbox/host_config_grants.zig`, `src/sandbox/profile.zig`, `src/tui/sandbox_card.zig`

## Branch

`cursor/fix-221-grok-launch-8968` (or agent’s `cursor/<name>-8968`)
