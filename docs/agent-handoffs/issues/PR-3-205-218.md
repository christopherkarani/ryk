# PR-3 handoff — #205 doctor walls + #218 start/doctor daemon contradiction

**Priority:** P2  
**Parallel vs banner/TUI:** yes  
**Internal:** **one agent / one PR** — shared `doctor.zig` remediation voice

## Problems (verified)

1. **#218:** `ryk start` hardcodes in-process Zig / marks daemon `.compatible` → prints `Daemon healthy`, next `ryk doctor`. Immediate `ryk doctor` probes companion binary → `daemon unavailable` + “Reinstall the complete ryk release.”
2. **#205:** Default doctor still renders `+---+` panels via `writeDefaultPanels` / `tui.render.panel`; companion-missing still owns Recommended next → reinstall.

## Acceptance

1. One honest daemon/capability line across start + doctor.
2. If in-process is enough: start must not claim companion-healthy; doctor must not send users to reinstall companion.
3. Doctor default output linear (drop walls); one next step.
4. Keep capability vs live-session honesty.
5. Do not reopen #145.

## Primary files

`src/cli/start.zig`, `src/cli/doctor.zig`, `src/cli/onboarding.zig`, `src/cli/daemon_errors.zig`, `src/tui/render.zig`

## Branch

`cursor/fix-205-218-daemon-honesty-8968`
