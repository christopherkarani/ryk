# PR-2 handoff — #217 TUI no-TTY fallback should be linear

**Priority:** P2  
**Parallel:** yes  
**Optional same-PR:** `history --live` (same bug class, unfiled)

## Problem (verified)

| Surface | Non-TTY / `--no-rich` / pipe | Exit |
|---------|------------------------------|------|
| `doctor --tui` | `using linear report` → linear | 0 |
| `replay --tui` | hard error “needs interactive colour terminal” | 2 |
| `history --live` | same hard error | 2 |
| packs / scan / allowlist | silent linear fallthrough | linear |

Shared gate `tui.output_policy.shouldEnterTui*` is fail-closed; only replay/history turn that into usage errors.

## Acceptance

1. `replay --tui` (and ideally `history --live`) fall through to linear; exit as linear would.
2. Invert rejection tests that assert exit 2.
3. Do **not** add new TUI surfaces; packs/scan/allowlist already comply.
4. Do not make doctor auto-open TUI.

## Primary files

`src/cli/replay.zig`, `src/cli/history.zig`, related tests; skim `src/cli/doctor_tui.zig` for the target contract.

## Branch

`cursor/fix-217-tui-fallback-8968`
