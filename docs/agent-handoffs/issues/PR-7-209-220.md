# PR-7 handoff — #209 update refuse + #220 installer homework

**Priority:** P2  
**Parallel:** yes (installer scripts; avoid conflicting with other packs)

## Problems (verified)

**#209 (REAL):** Installer treats any existing regular file without managed runtime markers as “non-ryk” and refuses overwrite unless `RYK_INSTALL_FORCE=1`. Never probes “is this binary ryk?”. `ryk update --force` does not set that env — help talks package-managed installs.

**#220 (PARTIAL):** Installer already runs `doctor --fix --from-install` on success. Dual doctor homework only when onboarding skipped. **Still real:** `print_activation` always prints `eval "$(… env)"` (even quiet); regression tests require it.

## Acceptance

1. Updating a real ryk binary succeeds without false refuse; if refuse remains, name why + one next (`--force` or env).
2. Success-path install/update: curl-and-done — no leftover homework; optional one host line (`ryk claude` / detected host).
3. Update installer regression tests accordingly.
4. Do not fold #221 / #207 / #205 / #211.

## Primary files

`scripts/install.sh`, `scripts/install.ps1`, `src/cli/update.zig`, `src/cli/help.zig`, installer regression scripts

## Branch

`cursor/fix-209-220-installer-8968`
