# Phase 2 — help / first-run copy

**Prefer after Phase 1** so “one next” matches start/doctor honesty.  
**Parallel inside phase:** PR-5 ∥ PR-6; PR-4 after or with PR-3.

## Packs

| Pack | Issues | Notes |
|------|--------|-------|
| PR-4 | #207 + #219 | First-run / public help nexts |
| PR-5 | #206 + #212 | Honest host lists |
| PR-6 | #211 + #214 | Help exits 0 (can run in Phase 0 parallel wave) |

## Contracts

1. First create / public help: what happened + **one** next (`ryk <agent>`), not start/doctor--fix dual nexts.
2. Stop/uninstall/plugin host lists match launch reality (no advertised `cursor` where launch forbids; honest grok/pi notes).
3. `ryk env --help` and bare `ryk policy explain` print real usage and exit 0 / usage with usage text (match `ryk test` bare feel).

## Already fixed (do not “fix”)

- #204 telemetry `--help` — already exits 0 on main
- #207 Ask-vs-DCG on `help start` — already corrected; only leftover nexts / Get protected remain
